Haml = require('haml')
events = require('events')
mixin = require('mixin')

class FileFinder extends Backbone.View
  id: "fileFinder"

  template: Haml """
  %input.filename
  .results
  """
  matchtemplate: Haml """
  .match(data-path=match.path)
    = match.projectPath()
  """

  events:
    "keyup input" : "_filenameChanged"
    "click .match": "_selectFile"

  render: ->
    @$el.html(@template())
    @

  _selectFile: (e) ->
    @emit('fileSelected', @model.getByPath($(e.target).data('path')))

  _filenameChanged: (e) ->
    searchString = $(e.target).val()
    return if searchString == ''
    matches = @model.filesMatching(searchString)

    @$el.find(".results").empty()
    for match in matches
      @$el.find(".results").append(@matchtemplate('match':match))

  show: ->
    @$el.show()
    @$el.find("input.filename").focus()
    @

mixin.include(FileFinder, events.EventEmitter::)
exports.FileFinder = FileFinder

