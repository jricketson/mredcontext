editor = require('editor_view') 

class EditorPaneView extends Backbone.View

  loadFile: (file) ->
    @$el.append @_newEditor(file)
    file.readContentsFromDisk()

  _newEditor: (file) ->
    new editor.EditorView(model:file).render().el

exports.EditorPaneView = EditorPaneView